# Roadmap Stage 5 — YAP SDK, Canvas and reference applications

Status: Work packages 1 and 2 are accepted on the target. Work package 3's
target measurements select indexed 4-bit Canvas for Paint; its repeated-launch
endurance check remains open. Work package 4 is next. Stage 4's remaining
physical gates continue in parallel; failed hardware checks take priority over
new features.

## Goal

Make OSEsp32 a platform on which an application can be created, validated,
packaged, copied to SD and run without editing the firmware. Prove the public
contract with several apps, especially Paint, while preserving the no-PSRAM
memory and capability boundaries.

## Work package 1 — project SDK

- Add a valid API 1.1 starter project with manifest, Lua, resource and README.
- Provide `python tools/yap.py new/check/build/inspect` on Windows and Linux.
- Refuse overwrites and invalid IDs/manifests before producing an artifact.
- Print API, mode, Lua quota, permissions, associations, resource count and
  output size so developers see the cost/authority they requested.
- Keep `yap_pack.py` as the deterministic low-level packer used by CI/tests.

Exit: a temporary project generated by `new` passes `check`, builds byte-for-byte
deterministically and passes `inspect` (**verified on host**), and its package
runs on the board (**verified by user on 2026-09-24**).

## Work package 2 — bounded UI API 1.2

Freeze the object and event contract before implementing widgets. Initial
limits must be explicit: maximum objects, text bytes, queued events, list rows,
timers and native allocations. IDs are app integers; the host owns actual LVGL
objects. Required primitives are label, button, toggle, text input, paged/list
container and system confirmation. Required events are tap, value change,
hold, swipe and bounded timer. All callbacks become queue entries consumed by
`ui.wait`; Lua never executes inside LVGL dispatch.

Implemented bounds: 24 widgets, eight queued events, four timers, six rows per
list and 12 list rows total. Windowed viewports are 300x148; fullscreen and
exclusive viewports are 320x240. Labels, buttons, toggles, text fields, lists,
confirmation, tap/change/hold/swipe/timer events and value lookup are exposed.

Exit: Calculator can be written without drawing its own controls, malformed or
excess object requests fail predictably, and API 1.0/1.1 examples are unchanged.
Host tests and firmware build pass; **Calculator touch/layout, normal exit,
emergency exit and the following legacy-app relaunch were verified by the user
on the target on 2026-09-24**.

Completed target check for work package 2:

1. Upload the current firmware and copy `build/calculator.yap` to
   `/OSEsp32/Apps`.
2. Confirm that the display and all 20 controls fit without clipping.
3. Enter `7 + 5 =` (result `12`), `3 / 0 =` (result `Error`), then `C`.
4. Confirm repeated taps update only the display without flicker or increasing
   delay; use the in-app `Exit` and verify immediate desktop restoration.
5. Relaunch once and verify the invisible two-second top-left emergency hold.

## Work package 3 — Canvas memory spike

Measure, do not guess, three single-buffer candidates for a 320x204 app area:

| Candidate | Pixel bytes | Main trade-off |
|---|---:|---|
| RGB565 | 130,560 | Direct color, likely too costly beside a useful Lua quota. |
| Indexed 8-bit | 65,280 + 1,024 palette = 66,304 | Works, but target largest block falls to an unsafe 20,468 bytes. |
| Indexed 4-bit | 32,640 + 64 palette = 32,704 | **Selected:** safe margin with 16 simultaneous colors. |

The Canvas is native and system-owned. Lua receives bounded drawing operations
and touch coordinates, not pixel tables or memory pointers. Track dirty
rectangles, cap work per update, and retain the emergency corner exit. A tiled
file-backed canvas is a measured fallback only; it is explicit paged data, not
virtual memory or a generic swap file.

The temporary API 1.3 `osesp32.canvas.probe()` experiment and
`canvas_probe.yap` are implemented. The probe is accepted only in exclusive
mode. It allocates one LVGL draw buffer, paints a visible pattern, moves a
24x24 square for 30 dirty-rectangle frames and returns the actual draw-buffer
size, allocation/fill time, free/minimum heap, largest free block and average/
maximum frame interval. `osesp32.canvas.release()` deletes both the LVGL object
and its buffer and reports the recovered heap. This diagnostic contract is not
the final Paint drawing API.

Initial target measurement procedure for work package 3:

1. Upload the current firmware and copy `build/canvas_probe.yap` to
   `/OSEsp32/Apps`.
2. Run **RGB**, record the displayed numbers, press **FREE**, and record the
   released free heap and largest block. Repeat for **I8** and **I4**.
3. Confirm each probe shows a patterned canvas and a moving square without a
   reset, corruption or stuck touch input.
4. Exit and relaunch the probe ten times, then run Calculator and
   `file_roundtrip.yap` to confirm complete shell/runtime restoration.
5. Record the results below. Choose the Paint format only after comparing the
   safe contiguous-memory margin and frame latency; allocation success alone
   is insufficient.

| Format | Buffer B | Free active | Largest active | Minimum free | Alloc/fill us | Frame avg/max ms | Free/largest after FREE |
|---|---:|---:|---:|---:|---:|---:|---:|
| RGB565 | allocation failed | — | — | — | — | — | 176,064 / 81,908 final baseline |
| I8 | 66,304 | 108,596 | 20,468 | 108,000 | 705 / 41,814 | 25 / 26 | not separately recorded |
| I4 | 32,704 | 144,500 | 49,140 | 143,896 | 547 / 48,370 | 25 / 26 | 176,064 / 81,908 final baseline |

Measurements were photographed on the ESP32-2432S028 on 2026-09-24. RGB565
returned `out_of_memory`. I8 rendered correctly but left only a 20,468-byte
largest block, which is too fragile once BMP row buffers, file dialogs and save
transactions are active. I4 preserved 35,904 more free bytes and a 28,672-byte
larger contiguous block than I8. Its one-time full fill was 6,556 us slower,
while the dirty-frame interval was identical. Paint therefore uses an I4 native
Canvas with a fixed 16-color palette; imported BMP pixels are quantized to that
palette and exported BMP pixels expand palette entries to 24-bit BGR. The final
drawing API will be additive rather than changing the diagnostic API 1.3.

The first endurance attempt exposed a lifecycle defect: starting I8 or I4
again usually closed the application on the second or third allocation. LVGL
9.5.0's `lv_canvas` destructor drops the address of its draw-buffer pointer
from the image cache rather than the draw buffer source. That can retain a
cache entry referring to freed pixel memory. OSEsp32 now displays its owned
draw buffer through a plain `lv_image`, explicitly drops the real buffer from
the cache, deletes the image, and only then destroys the buffer. It does not
patch the installed LVGL library. Runtime host coverage now performs three
consecutive probe/release continuations.

Remaining exit gate: the selected I4 Canvas must survive ten repeated exclusive
launch/exit cycles, followed by Calculator and `file_roundtrip.yap`, without a
falling released baseline or failed desktop restoration.

Retest the fix by uploading the current firmware, replacing
`canvas_probe.yap`, pressing **10x** once and checking for `I4 stress 10x OK`.
The displayed first and last `free`/`block` values should remain close. Then use
**EXIT**, relaunch once, and run Calculator and `file_roundtrip.yap`.

## Work package 4 — Paint

- Pencil, eraser, thickness, compact palette, clear and dirty indicator.
- Incremental uncompressed BMP 16/24/32-bit read with strict headers/bounds.
- Incremental interoperable 24-bit BGR write with four-byte row padding.
- System Open/Save, create-versus-replace permission and transactional close.
- Unsaved-exit confirmation; SD Retry/Close that preserves RAM canvas where
  possible and never claims a failed save succeeded.
- Own in-app Exit command; no system title/exit control in exclusive mode.

Exit: create/open/edit/save/reopen produces matching pixels on OSEsp32 and a PC;
interruption leaves an old or new complete BMP; repeated edits stay responsive.

## Work package 5 — reference suite

- Calculator: bounded widgets, state and event routing.
- YAP Notes: UTF-8, shared keyboard, private preferences and TXT documents.
- Paint: Canvas/files/removal reference.
- File viewer: chunked binary/text reads and resource use.
- Existing game port: timers, touch, Canvas and fullscreen/exclusive lifecycle.

Each example declares only capabilities it uses, documents normal/emergency
exit and has at least one host-side logic/package test. Built-in recovery apps
remain available even when SD/YAP execution fails.

## Work package 6 — application management

Decode a bounded package icon, create Windows-like desktop shortcuts, show
application metadata/API/mode/memory/permissions, and support uninstall with an
explicit choice to keep or delete `/OSEsp32/Data/<id>`. Removing/moving an app
must invalidate a remembered association. Replace root scanning with a compact
installer-maintained registry only when installation semantics and recovery are
defined; manual SD copy must still have a safe rescan path.

## Work package 7 — developer runner and diagnostics

Host tools can validate manifests/resources, run pure Lua state machines against
deterministic API stubs, inject errors and inspect logs. They cannot certify
touch geometry, rendering, ESP32 memory or timing. Package/API conformance and
hardware acceptance remain separate reports.

## Stage acceptance

1. `new → check → build → inspect` works in PowerShell and Bash without opening
   `.lua` through a file association.
2. API 1.0/1.1 packages still run, API 1.2 bounds hold, the experimental API
   1.3 Canvas probe remains exclusive-only, and unsupported future minors are
   rejected.
3. Calculator, YAP Notes, Paint, viewer and game run from `/OSEsp32/Apps`.
4. Paint round-trips BMP and survives cancel, failed write, removal and replace.
5. UI/Canvas limits fail with a controlled message, never corrupt shell state.
6. Windowed/fullscreen/exclusive normal and emergency exits restore desktop,
   wallpaper, keyboard, files and associations.
7. One hundred mixed launches stabilize in free heap and largest block.
8. Current build remains below 65% flash and 40% static RAM unless the partition
   and rollback/OTA policy are explicitly revised first.

## Out of scope

Native Xtensa applications, arbitrary LVGL access, generic SD swap, transparent
virtual memory, background Wi-Fi and an online store remain outside Stage 5.
PNG/scaling is optional only after Canvas memory acceptance.
